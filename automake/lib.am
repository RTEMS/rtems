##
##  $Id$
##

include $(RTEMS_ROOT)/make/leaf.cfg

define make-library
$(RM) $@
$(AR) $(ARFLAGS) $@ $^
$(RANLIB) $@
endef

TMPINSTALL_FILES = $(PROJECT_RELEASE)/lib

$(PROJECT_RELEASE)/lib:
	@$(mkinstalldirs) $@

.PRECIOUS: $(LIB)
