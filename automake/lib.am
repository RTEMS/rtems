##
##  $Id$
##

if RTEMS_CANONICALIZATION
else
AR = @AR@
RANLIB = @RANLIB@
endif

define make-library
$(RM) $@
$(AR) $(ARFLAGS) $@ $^
$(RANLIB) $@
endef

$(PROJECT_RELEASE)/lib:
	@$(mkinstalldirs) $@

TMPINSTALL_FILES = $(PROJECT_RELEASE)/lib

.PRECIOUS: $(LIB)
